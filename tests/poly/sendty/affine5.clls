proc main() {
  cut {
    sendty x(lint);
    send x(32);
    discard x
    |x: recvty A; recv coaffine ~A; affine A|
    recvty x(A);
    recv x(a);
    println("affine");
    affine x;
    println("use");
    use a;
    fwd x a
  }
};;
