proc main() {
  cut {
    sendty x(lint); println("use");
    send x(32);
    use x;
    println(x); ()
    |x: recvty A; recv coaffine ~A; affine A|
    recvty x(A);
    recv x(a);
    affine x;
    println("affine");
    use a;
    fwd x a
  }
};;
